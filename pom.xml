<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.0.6</version>
        <relativePath/>
    </parent>

    <groupId>com.nduyhai</groupId>
    <artifactId>guard-parent</artifactId>
    <version>1.0.1</version>
    <packaging>pom</packaging>
    <name>Guard :: Parent</name>
    <description>Annotation-driven method guard framework for Spring Boot</description>
    <url>https://github.com/nduyhai/guard</url>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>nduyhai</id>
            <name>Hai Nguyen Duy</name>
            <email>nduyhai@users.noreply.github.com</email>
            <url>https://github.com/nduyhai</url>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:git://github.com/nduyhai/guard.git</connection>
        <developerConnection>scm:git:ssh://github.com/nduyhai/guard.git</developerConnection>
        <url>https://github.com/nduyhai/guard</url>
        <tag>HEAD</tag>
    </scm>

    <modules>
        <module>guard-core</module>
        <module>guard-spring</module>
        <module>guard-spring-boot-autoconfigure</module>
        <module>guard-spring-boot-starter</module>
        <module>guard-samples</module>
    </modules>

    <properties>
        <java.version>25</java.version>
        <spring-modulith.version>2.0.6</spring-modulith.version>
        <guard.version>1.0.1</guard.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Internal modules -->
            <dependency>
                <groupId>com.nduyhai</groupId>
                <artifactId>guard-core</artifactId>
                <version>${guard.version}</version>
            </dependency>
            <dependency>
                <groupId>com.nduyhai</groupId>
                <artifactId>guard-spring</artifactId>
                <version>${guard.version}</version>
            </dependency>
            <dependency>
                <groupId>com.nduyhai</groupId>
                <artifactId>guard-spring-boot-autoconfigure</artifactId>
                <version>${guard.version}</version>
            </dependency>
            <dependency>
                <groupId>com.nduyhai</groupId>
                <artifactId>guard-spring-boot-starter</artifactId>
                <version>${guard.version}</version>
            </dependency>

            <!-- Spring Modulith 2.x targets Spring Framework 7 / Spring Boot 4 -->
            <dependency>
                <groupId>org.springframework.modulith</groupId>
                <artifactId>spring-modulith-bom</artifactId>
                <version>${spring-modulith.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-source-plugin</artifactId>
                    <executions>
                        <execution>
                            <id>attach-sources</id>
                            <goals>
                                <goal>jar-no-fork</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-javadoc-plugin</artifactId>
                    <configuration>
                        <doclint>none</doclint>
                        <quiet>true</quiet>
                    </configuration>
                    <executions>
                        <execution>
                            <id>attach-javadocs</id>
                            <goals>
                                <goal>jar</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>versions-maven-plugin</artifactId>
                    <version>2.21.0</version>
                    <configuration>
                        <generateBackupPoms>false</generateBackupPoms>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <argLine>-javaagent:${settings.localRepository}/org/mockito/mockito-core/${mockito.version}/mockito-core-${mockito.version}.jar</argLine>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-source-plugin</artifactId>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-javadoc-plugin</artifactId>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <version>3.2.8</version>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>sign</goal>
                                </goals>
                                <configuration>
                                    <!-- gpg.keyname / gpg.passphrase set via ~/.m2/settings.xml or -D flags -->
                                    <gpgArguments>
                                        <arg>--pinentry-mode</arg>
                                        <arg>loopback</arg>
                                    </gpgArguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                        <version>0.10.0</version>
                        <extensions>true</extensions>
                        <configuration>
                            <!-- Matches the <server id="central"> in ~/.m2/settings.xml -->
                            <publishingServerId>central</publishingServerId>
                            <!-- Set to true to release automatically; false leaves it in the "Pending" state for manual review -->
                            <autoPublish>true</autoPublish>
                            <waitUntil>published</waitUntil>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
